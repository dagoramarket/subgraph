{
  "name": "dagora-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --product hosted-service flametuner/Dagora",
    "create-local": "graph create --node http://localhost:8020/ dagora-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dagora-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dagora-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.33.0",
    "@graphprotocol/graph-ts": "^0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
